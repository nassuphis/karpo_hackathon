# Compiled C binaries built at deploy time (not tracked)
# These are rebuilt by deploy.sh from source .c files.
# Tracked binaries (ARM64 ELF committed to git) are NOT listed here.
gemini_key.txt

lambda/sweep
lambda/sweep_test
lambda/sweep_cli
lambda/sweep_cli_new
lambda/solver
lambda/pixassemble
lambda/raw2jpeg
lambda/lores_viewport
lambda/lores_viewport_*
lambda/imgpipe
lambda/bilevel
lambda/bilevel_raster
lambda/bilevel_merge
lambda/param_gen
lambda/tiff_compat
lambda/png_export
lambda/dz_export
lambda/wall_dz
lambda/sweep_cm
lambda/coeffs_bilevel_raster
lambda/roots2pix_mt
lambda/solve_proximity_hist_sectioned
lambda/solve_palette_chunk_mt
lambda/bilevel_section_raster
lambda/raw_to_bilevel
lambda/step_scores_to_palette_raw
# Shared lib directories (copied from Docker builds)
lambda/roots2pix_mt_lib/
lambda/solve_proximity_hist_sectioned_lib/
lambda/solve_palette_chunk_mt_lib/
# Local-only test binaries (never deployed)
lambda/*_local
lambda/*_test

# Lambda packaging
lambda/deploy.zip
lambda/*.zip

# Spreads
spreads/*.pdf

# Local ColorSpread PDF design/output scratch directory
test-pdf/

# Cross-compile layers (large, built separately)
lambda/layer-build/
lambda/layer-build-lapack/
lambda/layer-build-pdf/

# Python bytecode
__pycache__/
*.pyc

# Backup files
*.bak

# Test output binaries
lambda/*.bin
/tmp/

# Build/output artifacts
output/

# Large images
images/*.tif
images/*.png

# macOS
.DS_Store

# Node
node_modules

# Playwright test artifacts
test-results/
**/error-context.md
**/.last-run.json

# npm
package-lock.json

# Transpiler duplicates (poly_generated_funcs.h is the tracked version)
lambda/poly_generated_100.c
lambda/poly_generated_1000.c

# Bulk font-family download folders (size/filecount). The curated TTFs the
# book uses are TRACKED in fonts/ (13 files, ~840KB) — non-commercial project.
Canela_Collection/
tiempos-font-family/
courier-prime-font/
jetbrains-mono-font-family/
lyon-font/
sabon-similar/
sohne-font-family/

# CR18 oracle/bench output (Phase -1 of cr18-implementation-plan.md)
reports/

# knitr/LaTeX build artifacts (architecture-ui-critique.Rnw -> pdf)
*.aux
*.out
architecture-ui-critique.tex
architecture-ui-critique-2.tex
figure/

# Downloaded book/pdf artifacts (test outputs)
book*-content*.pdf
book*-cover*.pdf
book*-source*.zip
compute_*_pdf_*.pdf

# skybox sources (huge TIFF/EXR) stay local — only converted JPEGs are tracked
skybox/*.tif
skybox/*.tiff
skybox/*.exr
